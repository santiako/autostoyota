/**
 *
 * StyledRadioGlobal
 *
 */

import styled from 'styled-components';
import { createGlobalStyle } from 'styled-components';

const StyledRadioGlobal = createGlobalStyle`
    .radio-custom {
        display: none;
    }
`;

export default StyledRadioGlobal;
